package com.stah.api;

import android.util.Log;
import org.junit.Test;
import retrofit2.Response;
import rx.android.schedulers.AndroidSchedulers;
import rx.schedulers.Schedulers;

import java.util.List;

import static org.junit.Assert.*;

/**
 * Example local unit test, which will execute on the development machine (host).
 *
 * @see <a href="http://d.android.com/tools/testing">Testing documentation</a>
 */
public class ExampleUnitTest {
    @Test
    public void addition_isCorrect() {

        /*
        Response<List<GithubRespository>> repos = MixiApiClient.INSTANCE.fetchReposList();
        GithubRespository r = repos.body().get(0);
        System.out.println(r.getName());
        */




                /*
                .doOnNext {
            Log.d("TAG", "rx response=$it")
            //binding.textViewResponse.text = Gson().toJson(it)
        }
                .doOnError {
        }
                .doOnCompleted {
            Log.d(MainActivity.TAG, "rx response=")
        }
                .subscribe()
*/
    }
}